import {navbarListItemProps} from "@/app/(dashboard)/navbarlistitem";

const layoutOptions:navbarListItemProps[] = [{
    id: 1,
    name:'Add a Friend',
    Icon: 'AddUser',
    href: '/add',
}
]

export  default layoutOptions

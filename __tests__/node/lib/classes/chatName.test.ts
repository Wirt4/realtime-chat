import { ChatName } from '@/lib/classes/chatName/implementation';

describe('ChatName tests', () => {
    it('should be an instantiable class', () => {
        const chatName = new ChatName();
    })
})
